apply plugin: 'org.springframework.boot'

bootJar {
    baseName = 'discovery-service'
    version = '0.0.1'
}

dependencies {
    compile libs.springBootStarter,
            libs.eurekaServer,
            libs.gson,
            libs.swagger,
            libs.swaggerUI
}

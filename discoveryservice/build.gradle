apply plugin: 'org.springframework.boot'

bootJar {
    baseName = 'discoveryservice'
    version = '0.0.1'
}

dependencies {
    compile libs.springBootStarter,
            libs.eurekaServer,
            libs.gson
}
